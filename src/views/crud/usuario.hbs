<div class="container p-4">
    <div class="row">
         <div class="col-md-4 mx-auto">
             <div class="card">
                 <div class="card-body">
                     <form action="/edit/{{usuario.ID}}" method="POST">
                        <div class="form-group">
                            <input type="text" class="form-control" name="nombre" value="{{usuario.nombre}}" placeholder="Nombre" autofocus>
                        </div>
                        <div class="form-group">
                            <input type="text" class="form-control" name="apellido" value="{{usuario.apellido}}" placeholder="Apellido">
                        </div>
                        <div class="form-group">
                            <input type="number" class="form-control" name="edad" value="{{usuario.edad}}" placeholder="Edad">
                        </div>
                        <div class="form-group">
                            <input type="text" class="form-control" name="contrasena" value="{{usuario.contraseña}}" placeholder="contraseña">
                        </div>
                        <div class="form-group">
                            <button class="btn btn-success btn-block">
                                save
                            </button>
                        </div>
                     </form>
                 </div>
             </div>
         </div>
    </div>
</div>